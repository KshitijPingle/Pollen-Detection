# Pollen-Detection  
Pollen detection project aimed to detect and estimate pollen forage by honeybees  

## Quick Start  
Source important dataset files in all required locations, for example : in `train_bee_model.py` make sure all dataset paths are valid. Then, run `hierarchical_inference.py` to run the complete pollen detection pipeline.  



# README.md work in progress